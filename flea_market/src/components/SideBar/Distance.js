import { Link } from 'react-router-dom';

export default function Distance(props) {
    return (
        <div className="Distance">
        <Link to="/Distance">
        <h1>Distance</h1>
        </Link>
        </div>
    );
}